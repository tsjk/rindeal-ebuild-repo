<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
  <maintainer type="person">
    <email>dev.rindeal+gentoo-overlay@gmail.com</email>
    <name>Jan Chren (rindeal)</name>
  </maintainer>
  <use>
    <!--     doc -->
    <!--     test -->
    <!--     nls -->
    <!--     static-libs -->
    <flag name="assert">Toggle assertions</flag>
    <flag name="symvers">Library symbol versioning</flag>
    <flag name="largefile">support for large files</flag>
    <flag name="rpath">Hardcode runtime library paths</flag>

<!--     <flag name="caps">build setpriv helper (run programs with diff capabilities)</flag> -->
<!--     <flag name="cramfs">build mkfs/fsck helpers for cramfs filesystems</flag> -->
<!--     <flag name="fdformat">build fdformat (floppy disk format)</flag> -->
<!--     <flag name="kill">build the kill program</flag> -->
<!--     <flag name="pam">build runuser helper</flag> -->
<!--     <flag name="suid"> -->
<!--       install mount/umount as setuid so non-root users may mount/umount devices, -->
<!--       and wall/write as setgid so non-root users can notify other users -->
<!--     </flag> -->
<!--     <flag name="tty-helpers">install the mesg/wall/write tools for talking to local users</flag> -->


    <!-- ### Libraries -->
    <flag name="libblkid">libblkid(3) - block device identification library</flag>
    <flag name="libfdisk">libfdisk library - manipulation with partition tables</flag>
    <flag name="libmount">libmount library - parse of /etc/fstab, /etc/mtab and
/proc/self/mountinfo files, management of the mtab file, evaluation of mount options, etc.</flag>
    <flag name="libmount-support-mtab">Support /etc/mtab (default is use /proc/self/mountinfo)</flag>
    <flag name="libsmartcols">libsmartcols library - smart adaptive formatting of tabular data</flag>
    <flag name="libuuid">uuid(3) library - DCE compatible Universally Unique Identifier library</flag>
    <flag name="libuuid-force-uuidd">support uuidd even though the daemon is not built</flag>


    <!-- ### Utilities -->
    <flag name="agetty">agetty(8) - (alternative Linux getty) Open a terminal and set its mode</flag>
    <flag name="bfs">mkfs.bfs(8) - make an SCO bfs filesystem</flag>
    <flag name="cal">cal(1), ncal(1) - displays a calendar and the date of Easter</flag>
    <flag name="chfn-chsh">chfn(1) - change real user name and information;
                            chsh(1) - change login shell</flag>
    <flag name="chfn-chsh-password">do not require the user to enter the password in chfn and chsh</flag>
    <flag name="chsh-only-listed">chsh: allow only shells listed in /etc/shells</flag>
    <flag name="cramfs">fsck.cramfs(8), mkfs.cramfs(8) - fsck/make compressed ROM file system</flag>
    <flag name="eject">eject(1) - eject removable media</flag>
    <flag name="fallocate">fallocate(1) - preallocate/deallocate space to/from a file</flag>
    <flag name="fdformat">fdformat(8) - low-level format a floppy disk; obsolete</flag>
    <flag name="fsck">fsck(8) - check and repair a Linux filesystem</flag>
    <flag name="hwclock">hwclock(8) - query and set the hardware clock (RTC)</flag>
    <flag name="ipcrm">ipcrm(1) - remove certain IPC resources</flag>
    <flag name="ipcs">ipcs(1) - show information on IPC facilities</flag>
    <flag name="kill">kill(1) - send a signal to a process; better alternative to the one from <pkg>sys-process/procps</pkg></flag>
    <flag name="last">last(1), lastb(1) - show listing of last logged in users</flag>
    <flag name="line">line(1) - read one line</flag>
    <flag name="logger">logger(1) - a shell command interface to the syslog(3) system log module</flag>
    <flag name="login">login(1) - begin session on the system</flag>
    <flag name="login-chown-vcs">let login(1) chown /dev/vcsN</flag>
    <flag name="login-stat-mail">let login(1) stat() the mailbox</flag>
    <flag name="losetup">losetup(8) - set up and control loop devices</flag>
    <flag name="lslogins">lslogins(1) - display information about known users in the system</flag>
    <flag name="mesg">mesg(1) - display (or do not display) messages from other users; obsolete</flag>
    <flag name="minix">fsck.minix(8), mkfs.minix(8) - fsck/make Minix filesystem</flag>
    <flag name="more">more(1) - file perusal filter for crt viewing</flag>
    <flag name="mount">mount(8), umount(8) - mount/unmount filesystem</flag>
    <flag name="mountpoint">mountpoint(1) - see if a directory or file is a mountpoint</flag>
    <flag name="newgrp">newgrp(1) - log in to a new group</flag>
    <flag name="nologin">nologin(8) - politely refuse a login</flag>
    <flag name="nsenter">nsenter(1) - run program with namespaces of other processes</flag>
    <flag name="partx">addpart(8) - tell the kernel about the existence of a partition;
                        delpart(8) - tell the kernel to forget about a partition;
                        partx(8) - tell the kernel about the presence and numbering of on-disk partitions</flag>
    <flag name="pg">pg(1) - browse pagewise through text files; obsolete</flag>
    <flag name="pg-bell">let pg(1) not ring the bell on invalid keys</flag>
    <flag name="pivot_root">pivot_root(8) - change the root filesystem</flag>
    <flag name="raw">raw(8) - bind a Linux raw character device</flag>
    <flag name="rename">rename(1) - rename files</flag>
    <flag name="reset">reset(1) - terminal initialization</flag>
    <flag name="runuser">runuser(1) - run a command with substitute user and group ID</flag>
    <flag name="schedutils">chrt(1) - manipulate the real-time attributes of a process;
                              ionice(1) - set or get process I/O scheduling class and priority;
                              taskset(1) - set or retrieve a process's CPU affinity</flag>
    <flag name="setpriv">setpriv(1) - run a program with different Linux privilege settings</flag>
    <flag name="setterm">setterm(1) - set terminal attributes</flag>
    <flag name="su">su(1) - change user ID or become superuser</flag>
    <flag name="sulogin">sulogin(8) - single-user login</flag>
    <flag name="switch_root">switch_root(8) - switch  to  another filesystem as the root of the mount tree</flag>
    <flag name="tunelp">tunelp(8) - set various parameters for the lp device (parallel line printer); obsolete</flag>
    <flag name="ul">ul(1) - do underlining; obsolete</flag>
    <flag name="unshare">unshare(1) - run program with some namespaces unshared from parent</flag>
    <flag name="utmpdump">utmpdump(1) - dump UTMP and WTMP files in raw format</flag>
    <flag name="vipw">vipw(8), vigr(8) - edit the password, group, shadow-password or shadow-group file</flag>
    <flag name="wall">wall(1) - write a message to all users; obsolete</flag>
    <flag name="wdctl">wdctl(8) - show hardware watchdog status</flag>
    <flag name="write">write(1) - write to another user; obsolete</flag>
    <flag name="zramctl">zramctl(8) - set up and control zram devices</flag>


    <!-- ### Other -->
    <flag name="uuidd">uuid daemon</flag>
    <flag name="pylibmount">Python bindings for the libmount library</flag>


    <!-- ### Options -->
    <flag name="colors-default">colorize output from utils by default</flag>
    <flag name="plymouth-support">Care about plymouth in sylogin(8) and agetty(8)</flag>
    <flag name="sulogin-emergency-mount">Use emergency mount of /dev and /proc for sulogin</flag>
    <flag name="use-tty-group">install wall and write setgid tty</flag>
    <flag name="usrdir-path">use only /usr paths in PATH env. variable (recommended on systems with /bin -> /usr/bin symlinks)</flag>

    <!-- ### Optional lib deps -->
    <flag name="btrfs">build with support for btrfs</flag>
    <flag name="cap-ng">compile with libcap-ng</flag>
    <flag name="libpython">Compile with python support; required for pylibmount</flag>
    <flag name="libz">compile with libz</flag>
    <flag name="readline">compile with GNU Readline support</flag>
    <flag name="slang">compile cfdisk with slang rather than ncurses</flag>
    <flag name="smack">SMACK security subsystem support</flag>
    <flag name="tinfo">compile with tinfo (low level lib from ncurses); obsolete</flag>
    <flag name="user">compile with libuser (remote chsh); obsolete</flag>
    <flag name="utempter">compile script(1) with libutempter</flag>
    <flag name="util">Compile with libutil, part of libc</flag>

    <!-- ### Extras -->
    <flag name="fdisk">fdisk(8) - manipulate disk partition table</flag>
    <flag name="sfdisk">sfdisk(8) - display or manipulate a disk partition table; script-oriented</flag>
    <flag name="cfdisk">cfdisk(8) - display or manipulate a disk partition table; curses-based</flag>

    <flag name="uuidgen">uuidgen(1) - create a new UUID value</flag>
    <flag name="blkid">blkid(8) - locate/print block device attributes</flag>
    <flag name="findfs">findfs(8) - find a filesystem by label or UUID</flag>
    <flag name="wipefs">wipefs(8) - wipe a signature from a device</flag>
    <flag name="findmnt">findmnt(8) - find a filesystem</flag>

    <flag name="mkfs">mkfs(8) - build a Linux filesystem</flag>
    <flag name="isosize">isosize(8) - output the length of an iso9660 filesystem</flag>
    <flag name="fstrim">fstrim(8) - discard unused blocks on a mounted filesystem</flag>
    <flag name="swapon">swapon(8), swapoff(8) - enable/disable devices and files for paging and swapping</flag>
    <flag name="lsblk">lsblk(8) - list block devices</flag>
    <flag name="lscpu">lscpu(1) - display information about the CPU architecture</flag>

    <flag name="chcpu">chcpu(8) - configure CPUs in a multi-processor system</flag>

    <flag name="swaplabel">swaplabel(8) - print or change the label or UUID of a swap area</flag>
    <flag name="mkswap">mkswap(8) - set up a Linux swap area</flag>

    <flag name="look">look(1) - display lines beginning with a given string</flag>
    <flag name="mcookie">mcookie(1) - generate magic cookies for xauth</flag>
    <flag name="namei">namei(1) - follow a pathname until a terminal point is found</flag>
    <flag name="whereis">whereis(1) - locate the binary, source, and manual page files for a command</flag>
    <flag name="getopt">getopt(1) - parse command options (enhanced)</flag>
    <flag name="blockdev">blockdev(8) - call block device ioctls from the command line</flag>
    <flag name="prlimit">prlimit(1) - get and set process resource limits</flag>
    <flag name="lslocks">lslocks(8) - list local system locks</flag>

    <flag name="flock">flock(1) - manage locks from shell scripts</flag>
    <flag name="ipcmk">ipcmk(1) - make various IPC resources</flag>

    <flag name="lsipc">lsipc(1) - show information on IPC facilities currently employed in the system</flag>
    <flag name="lsns">lsns(8) - list namespaces</flag>
    <flag name="renice">renice(1) - alter priority of running processes</flag>
    <flag name="setsid">setsid(1) - run a program in a new session</flag>
    <flag name="readprofile">readprofile(8) - read kernel profiling information</flag>
    <flag name="dmesg">dmesg(1) - print or control the kernel ring buffer</flag>
    <flag name="ctrlaltdel">ctrlaltdel(1) - set the function of the Ctrl-Alt-Del combination</flag>
    <flag name="fsfreeze">fsfreeze(8) - suspend access to a filesystem (Ext3/4, ReiserFS, JFS, XFS)</flag>
    <flag name="blkdiscard">blkdiscard(8) - Discard the content of sectors on a device</flag>
    <flag name="ldattach">ldattach(8) - attach a line discipline to a serial line</flag>
    <flag name="rtcwake">rtcwake(8) - enter a system sleep state until specified wakeup time</flag>
    <flag name="setarch">setarch(8) - change reported architecture in new program environment and set personality flags</flag>
    <flag name="script">script(1) - make typescript of terminal session</flag>
    <flag name="scriptreplay">scriptreplay(1) - play back typescripts, using timing information</flag>
    <flag name="col">col(1) - filter reverse line feeds from input</flag>
    <flag name="colcrt">colcrt(1) - filter nroff output for CRT previewing</flag>
    <flag name="colrm">colrm(1) - remove columns from a file</flag>
    <flag name="column">column(1) - columnate lists</flag>
    <flag name="hexdump">hexdump(1) - display file contents in hexadecimal, decimal, octal, or ascii</flag>
    <flag name="rev">rev(1) - reverse lines characterwise</flag>
    <flag name="tailf">tailf(1) - follow the growth of a log file; obsolete</flag>

    <flag name="ionice">ionice(1) - set or get process I/O scheduling class and priority</flag>
    <flag name="taskset">taskset(1) - set or retrieve a process's CPU affinity</flag>
    <flag name="chrt">chrt(1) - manipulate the real-time attributes of a process</flag>
  </use>
</pkgmetadata>
